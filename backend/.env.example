# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าให้ตรงกับ environment
# cp .env.example .env

# Database (MySQL/MariaDB)
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/DATABASE_NAME"
DATABASE_USER="USER"
DATABASE_PASSWORD="PASSWORD"
DATABASE_NAME="DATABASE_NAME"
DATABASE_HOST="localhost"
DATABASE_PORT=3306
DATABASE_CONNECTION_LIMIT=20

# App
NODE_ENV=development
PORT=4000

# CORS (ถ้าว่าง ใช้ default localhost และ host.docker.internal สำหรับ Docker)
# เมื่อ deploy ที่ IP/โดเมนจริง เช่น http://10.11.9.84:4100/smart-cabinet-cu ให้ใส่ origin นั้น
# CORS_ORIGIN=http://10.11.9.84:4100,http://localhost:4100,https://your-domain.com

# JWT
JWT_SECRET=your-jwt-secret-change-in-production
JWT_REFRESH_SECRET=your-jwt-refresh-secret
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d


# Print Sticker (TCP RAW — ใช้ hostname/IP; POST print-test ใช้เป็น default ถ้าไม่ส่ง host ใน body)
PRINT_STICKER_HOST=http://172.20.220.139
PRINT_STICKER_PORT=9100