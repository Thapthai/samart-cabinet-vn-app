# คัดลอกเป็น .env แล้วแก้ค่าให้ตรงกับ environment
# cp .env.example .env

# NextAuth
NEXTAUTH_SECRET=your-nextauth-secret-change-in-production
NEXTAUTH_URL=http://localhost:4100/smart-cabinet-cu
NEXT_PUBLIC_BASE_PATH=/smart-cabinet-cu

# Backend API (port 4000 เมื่อ deploy ด้วย Docker Compose)
# NEXT_PUBLIC_API_URL = เบราว์เซอร์เรียกใช้ (ใช้ localhost:4000 เมื่อเปิดแอปจากเครื่องเดียวกับ Backend)
NEXT_PUBLIC_API_URL=http://localhost:4000/smart-cabinet-cu/api/v1
# BACKEND_API_URL = เฉพาะเมื่อ Frontend รันใน Docker — ให้ NextAuth (server-side) เรียก Backend ได้ (เช่น host.docker.internal:4000)
# BACKEND_API_URL=http://host.docker.internal:4000/smart-cabinet-cu/api/v1

# Firebase (ใส่ค่าจาก Firebase Console)
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# ค่าเริ่มต้นเครื่องปริ้นสติกเกอร์ (backend .env) — หน้า /staff/management/print-sticker
# PRINT_STICKER_HOST=http://172.20.220.139
# PRINT_STICKER_PORT=9100